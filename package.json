{
  "name": "frontend-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run --prefix packages/server-hapi debug\" \"npm run --prefix packages/react-ui dev\" \"npm run --prefix packages/vue-ui dev\"",
    "build": "concurrently \"npm run --prefix packages/server-hapi build\" \"npm run --prefix packages/react-ui build\" \"npm run --prefix packages/vue-ui build\"",
    "start": "npm run --prefix packages/server-hapi start"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "7.x"
  }
}
